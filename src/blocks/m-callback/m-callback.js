(() => {

	/* $('body').on('change', '.m-callback__agree-check[type="checkbox"]', function(e) {
		const $self = $(this);
		$self.parents('form').find('button[type="submit"]').prop('disabled', !$self.is(':checked'));
	}); */

})();